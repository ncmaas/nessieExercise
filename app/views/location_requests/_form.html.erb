<%= form_for(@location_request) do |f| %>
  <% if @location_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_request.errors.count, "error") %> prohibited this location_request from being saved:</h2>

      <ul>
      <% @location_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lat %><br>
    <%= f.text_field :lat %>
  </div>
  <div class="field">
    <%= f.label :long %><br>
    <%= f.text_field :long %>
  </div>
  <div class="field">
    <%= f.label :radius %><br>
    <%= f.number_field :radius %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
